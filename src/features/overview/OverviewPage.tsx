import PagePlaceholder from '../../components/PagePlaceholder'

/** 개요 — 항공 요약·정책 카드(추후 슬라이스). */
export default function OverviewPage() {
  return <PagePlaceholder title="개요" subtitle="한눈에 보는 여행" />
}
